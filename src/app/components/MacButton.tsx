import { ReactElement } from "react";

export const MacButton = ({
  icon,
  text,
  hoverIcon,
}: {
  icon: ReactElement;
  text: string;
  hoverIcon: ReactElement;
}) => {
  return <div>Button</div>;
};
